# tuteTaskPromise
## Promise based Task
  * JavaScript function that takes two numbers as input and returns a Promise.
## 📖 Description
* **The Motivation**: learn and understand use of Promise?
* **The Problem**:handle the synchronous behaviour into Asynchronous?

## 🛠️ Built With

List the core stack used to build this software:
* html
* javaScript


### push code on github Steps

1.git initialisation
 ```bash
   git init   
   ```
    
2.git remote add the repository:
    ```bash
       git remote add origin "https://github.com/Rubi-1997/tuteTaskPromise"
       
  
   
3.check status:
      ```bash
         git status
        
4.add all changes or file on git
 ```bash
   git add 
   ```
5.comment
   ```bash
      git commit -m "changes"
      ```
6.Push code on github
    ```bash
       git push 
         ```
    
     
     
 
